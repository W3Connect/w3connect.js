{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/wallet",
      "packages/core",
      "package/fortmatic",
      "package/metamask",
      "packages/connect",
      "packages/*"
    ],
    "nohoist": []
  },
  "version": "0.0.0",
  "scripts": {
    "test": "yarn workspaces run test",
    "build": "yarn workspaces run build"
  },
  "name": "w3connect.js",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
