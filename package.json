{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/wallet",
      "packages/*"
    ],
    "nohoist": []
  },
  "version": "0.0.0",
  "scripts": {
    "test": "yarn workspaces run test",
    "build": "yarn workspaces run build"
  }
}
