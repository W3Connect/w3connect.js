{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": []
  },
  "version": "0.0.0",
  "scripts": {
    "test": "yarn workspaces run test",
    "install": "yarn workspaces run install"
  }
}
